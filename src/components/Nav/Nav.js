import React from 'react';
import PropTypes from 'prop-types';
import './Nav.scss';

const Nav = () => (
  <div className="Nav">
    Nav Component
  </div>
);

Nav.propTypes = {};

Nav.defaultProps = {};

export default Nav;
